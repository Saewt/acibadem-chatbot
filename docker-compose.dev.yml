services:
  web:
    volumes:
      - ./webapp:/app
      - ${ACIBADEM_DATASET_HOST_ROOT:-./data/acibadem-dataset}:/data/acibadem-dataset:ro
    command: >
      sh -c "python manage.py migrate &&
             exec python manage.py runserver 0.0.0.0:8000"
